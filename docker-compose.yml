version: "3.9"

services:
  aavail-api:
    build: .
    container_name: aavail-api
    ports:
      - "5000:5000"
    volumes:
      - ./models:/app/models
      - ./logs:/app/logs
      - ./data:/app/data